---
id: core_scripts_regional_map
type: regional_map
zone: core/scripts
---

# 🗺️ _REGIONAL_MAP — $OMNICLAW_ROOT\core\scripts

## ⚙️ Active Core Utilities

| Script Name | Purpose |
|---|---|
| `generate_ecosystem_root_map.py` | Auto-generates the master map for the 7 external layers of the ecosystem. |
| `generate_skill_maps.py` | (Legacy) Utility for generating skill categorization nodes. |
| `skill_assimilator.py` | Validates `ecosystem/skills`, optionally repairs metadata in explicit repair mode, and compiles the `SKILL_REGISTRY.json`. |
| `tool_loader.py` | Deep crawler that validates `ecosystem/tools`, builds the `TOOL_REGISTRY.json`, and updates the native tool map. |

## System Identity
- `_DIR_IDENTITY.md`
